<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Client" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Coffee Theme settings -->
        <item name="colorPrimary">@color/coffee_primary</item>
        <item name="colorSecondary">@color/coffee_secondary</item>
        <item name="colorTertiary">@color/coffee_accent</item>
        <item name="android:windowBackground">@color/coffee_background</item>
        <item name="android:textColorPrimary">@color/coffee_text</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/coffee_primary</item>
    </style>

    <style name="Theme.Client" parent="Base.Theme.Client" />
</resources>